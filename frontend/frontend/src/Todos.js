import React from 'react';

/*
const Todos = ({todos}) => {
    const todoList = todos.length ? (
        todos.map(todo =>  {
            return(
                <div className="Collection-item" > </div>
            )

        })
    )
}

*/
